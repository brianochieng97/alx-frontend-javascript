const function taskFirst() {
	var = 'I Prefer const when I can,';
	return task;
}

let function getLast() {
	return ' is okay';
}

const function taskNext() {
	var combination = 'But sometimes let';
	combination += getLast();
	return combination;
}


console.log(`${taskFirst()} ${taskNext()}`);

